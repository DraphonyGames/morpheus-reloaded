using UnityEngine;
using System.Collections;
namespace Assets.scripts.Enemy.DragonScripts
{
    /// <summary>
    /// point where the boss should launch
    /// </summary>
    public class LaunchZone : MonoBehaviour
    {
        /// <summary>
        /// next launch zone
        /// </summary>
        public GameObject nextLaunchZone;

        // Use this for initialization
        void Start()
        {
        }

        // Update is called once per frame
        void Update()
        {
        }
    }
}